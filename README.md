# senseixiaomisensei-sudo.github.io

个人主页,托管于 GitHub Pages。

## 访问

公开访问地址(所有人可访问):

```
https://senseixiaomisensei-sudo.github.io/
```

## 本地预览

直接用浏览器打开 `index.html`,或在目录下起一个静态服务器:

```bash
python -m http.server 8000
# 然后访问 http://localhost:8000
```

## 目录结构

```
.
├── index.html   # 单文件站点(内联 CSS / JS)
└── README.md
```

## 部署方式

推送到 `main` 分支后,GitHub Pages 自动构建并发布。
