# 2026-09-22 界面与运行修复

保留 08d62b3 的风格切换、工作台、原声波形和所有原有功能。

- 参考视频中的粒子轮廓，为空闲工作台加入指针响应粒子；上传后仍显示真实音频波形。
- 扩展上传区、结果区、角色卡、模式按钮的透明材质、折射高光和边缘反射。
- 手机流程条移到底部安全区并保留页面底部空间；桌面顶部偏移随实际导航高度变化。手动滚动同步步骤，减少动效及透明度设置有回退。
- 本地使用与 CPU 后端匹配的标准 WASM，避免不支持 JSEP 的构建误选 WebGPU。两个大型编码器顺序初始化，降低同时编译内存压力，并分别显示加载阶段。模型缓存保留。
- 云端重试不再向 GitHub 静态站点 POST；所有长度的结果下载都有流读取期限，断流重读已有任务，不重复提交音频。
- 本机恢复脚本纳入版本控制，排除错误日志的 api.trycloudflare.com，先验证隧道健康再更新 Worker。启用系统代理的主机可通过仅监听回环地址的固定注册桥访问 Cloudflare 注册 API。

## 验证

139 项 Node 检查通过，包括响应体停滞取消、完整读取和静态域名回退排除。
10 个页面在 1440、768、390 像素宽度检查通过，零页面脚本错误；风格切换、选角色、调音、波形及原声试听交互通过。
桌面 Chrome 的纯 CPU/WASM 本地流程实际转换星野样本成功，约 33 秒；手机实机内存上限仍需实际设备验证，手机尺寸模拟不等同实机性能。

## 云端尚未恢复

本机 GPU healthz 正常，公开入口最初返回 530，静态站点误重试又返回 405。注册 API 经本机既有代理可达，但 Cloudflare 边缘节点的 TCP/UDP 7844 连接均超时，导致隧道尚不能提供服务。健康检查阻止错误地址覆盖线上入口。需要网络允许隧道连接后，现有看门狗才能完成自动恢复；不能将本次部署视为云端端到端验收成功。

验收文件位于 `E:\大肥鱼\界面与运行修复\20260922\验收`；全站截图位于 `E:\大肥鱼\test-artifacts\liquid-ui-20260920`（复用原验收脚本目录，本次已重新生成）。
